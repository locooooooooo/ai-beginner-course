# This file is editable lesson text. Run scripts/generate_lessons.py after changing it.
# Text fields use TOML triple-quoted strings so Chinese punctuation and line breaks are safe.

chapter_no = 3
no = 12
slug = """lesson-12-rag-and-qa"""
title = """搜索、知识库与问答"""
goal = """理解为什么有些问题必须接知识库，而不能只靠模型本身。"""
big_question = """为什么模型很强，却回答不好企业内部事实和最新规则？"""
exercise = """选一个业务场景，说明为什么必须接知识库，而不能只问模型。"""

keyPoints = [
  """模型参数不等于企业知识库""",
  """RAG 解决的是“先检索，再回答”""",
  """企业问答的关键是数据来源可追溯""",
]

structure = [
  """为什么模型会答错企业事实""",
  """RAG 的基本思路""",
  """FAQ 与知识库问答""",
  """企业落地注意点""",
]

cases = [
  """客服 FAQ""",
  """内部制度问答""",
  """商品政策检索""",
]

ppt = [
  """封面：知识库与问答""",
  """为什么只靠模型不够""",
  """RAG 基本逻辑""",
  """FAQ 场景""",
  """练习页""",
]

first_principles = [
  """先把问题分成两类：模型可以推断的问题，和必须查资料的问题。""",
  """企业事实、最新政策、内部制度属于后者，所以需要外部知识源。""",
  """RAG 的本质是把“模型的语言处理能力”和“企业的资料库”通过检索链路组合起来。""",
]

atomic_truths = [
  """本质一：模型参数不是你的企业数据库。""",
  """本质二：事实类问答的关键不是文采，而是来源。""",
  """本质三：可追溯性比流畅性更重要。""",
]

method = [
  """先识别哪些问题必须引用文档、制度或数据库。""",
  """为这些问题接知识库或检索层。""",
  """让答案始终带着来源返回。""",
]

anti_patterns = [
  """把企业知识直接寄希望于模型参数。""",
  """没有来源也敢直接回答内部问题。""",
  """只做检索，不做答案结构设计。""",
]

[deep_note.meta]

[deep_note]
plain = """模型本身不等于你的企业知识库。凡是涉及最新政策、内部制度、商品规则、客服口径的问题，都应该先查资料再回答，并把来源带出来，这就是 RAG 的基本价值。"""
checks = [
  """这个问题是否必须查资料？""",
  """答案有没有标出来源？""",
  """知识库里是否有过期或冲突文件？""",
]

[[deep_note.teacher_script]]
title = """先分清能推理和必须查资料"""
body = """写一段通用说明可以靠模型，回答公司退换货规则必须查内部文档。"""

[[deep_note.teacher_script]]
title = """答案要带来源"""
body = """企业问答最怕流畅但没依据，所以要让 AI 标出引用文档、章节和更新时间。"""

[[deep_note.teacher_script]]
title = """知识库也要维护"""
body = """RAG 不是把文件丢进去就结束，过期文档、重复文档、冲突规则都会影响答案。"""

[deep_note.walkthrough]
scene = """做一个售后政策问答"""
steps = [
  """收集官方售后政策、FAQ 和例外条款。""",
  """把问题交给知识库检索，不让模型脱离外部资料直接生成答案。""",
  """要求答案包含依据、适用范围和不确定项。""",
  """客服主管抽查高频问题答案。""",
]

[deep_note.worked_example]
