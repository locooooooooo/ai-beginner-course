# This file is editable lesson text. Run scripts/generate_lessons.py after changing it.
# Text fields use TOML triple-quoted strings so Chinese punctuation and line breaks are safe.

chapter_no = 3
no = 11
slug = """lesson-11-image-and-video"""
title = """图片与视频生成"""
goal = """理解图像模型和视频模型能做什么，以及怎样在业务里正确使用。"""
big_question = """为什么图像和视频生成看起来更直观，却更容易在业务里踩坑？"""
exercise = """为一个活动海报写一段图像生成提示词，并补充品牌规范要求。"""

keyPoints = [
  """文生图适合海报、商品图、广告图""",
  """改图适合局部替换和风格统一""",
  """视频模型更适合短视频脚本和镜头生成""",
]

structure = [
  """图像生成的典型任务""",
  """视频生成的典型任务""",
  """提示词与素材准备""",
  """版权和合规边界""",
]

cases = [
  """电商海报图""",
  """商品场景图""",
  """广告片镜头板""",
]

ppt = [
  """封面：图片与视频""",
  """图像模型做什么""",
  """视频模型做什么""",
  """电商案例""",
  """风险与边界""",
]

first_principles = [
  """视觉生成的底层问题先是“要表达什么”，不是“用哪个模型”。""",
  """图像和视频的失败，通常来自品牌规范、事实信息和版权边界没有提前定义。""",
  """业务里真正有价值的是稳定产出合规素材，而不是偶尔生成一张惊艳图。""",
]

atomic_truths = [
  """本质一：视觉内容首先是商业表达，其次才是生成技术。""",
  """本质二：文字、价格、规格、Logo 这些业务信息比画面氛围更关键。""",
  """本质三：视觉类结果必须更重视人工核校。""",
]

method = [
  """先锁定用途、尺寸、品牌规范和必须保真的信息。""",
  """再决定是生成、改图还是做视频镜头。""",
  """所有对外素材最后都要进行人工核校。""",
]

anti_patterns = [
  """只看好不好看，不看信息是否正确。""",
  """忽略版权、商标、肖像和平台规范。""",
  """把模型生成的视觉当作直接可发的终稿。""",
]

[deep_note.meta]

[deep_note]
plain = """图片和视频生成看起来直观，但业务里不能只看好不好看。你要先写清用途、尺寸、品牌规范、必须准确的信息和不能出现的内容，再生成或改图，最后进行人工核校。"""
checks = [
  """我有没有写清图片用途和尺寸？""",
  """哪些信息必须人工确认？""",
  """这个任务更适合生成、改图还是视频分镜？""",
]

[[deep_note.teacher_script]]
title = """先写视觉任务书"""
body = """图像提示词不是形容词堆叠，而是用途、主体、场景、风格、尺寸、禁忌和验收标准。"""

[[deep_note.teacher_script]]
title = """区分生成和改图"""
body = """从零做海报适合生成，替换背景、统一风格、修局部更适合改图，不要所有任务都文生图。"""

[[deep_note.teacher_script]]
title = """对外素材必须人工核校"""
body = """文字、Logo、价格、规格、肖像、版权都可能出问题，对外发布前必须进行人工核校。"""

[deep_note.walkthrough]
scene = """生成一张活动海报主视觉"""
steps = [
  """明确用途：618 活动首页横幅，尺寸 16:9。""",
  """写清主体、场景、品牌色、留白区域。""",
  """禁止生成错误文字、假 Logo、真实人物肖像。""",
  """人工核校商品外观、文字区域和安全合规风险。""",
]

[deep_note.worked_example]
