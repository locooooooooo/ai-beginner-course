# This file is editable lesson text. Run scripts/generate_lessons.py after changing it.
# Text fields use TOML triple-quoted strings so Chinese punctuation and line breaks are safe.

chapter_no = 4
no = 15
slug = """lesson-15-mcp"""
title = """什么是 MCP"""
goal = """理解 MCP 为什么是 AI 真正连接业务系统的重要一层。"""
big_question = """为什么很多 AI 输出看起来很自然，但仍然接触不到你的真实业务数据和系统动作？"""
exercise = """为一个电商业务流程列出至少 4 个需要通过 MCP 连接的系统。"""

keyPoints = [
  """MCP 是连接外部系统的统一方式""",
  """没有连接，AI 很多时候只能停留在建议层，无法执行受控动作""",
  """MCP 的价值在于工具发现、资源读取和动作执行""",
]

structure = [
  """为什么需要统一连接层""",
  """MCP 能连什么""",
  """业务系统对接示例""",
  """安全和权限边界""",
]

cases = [
  """连接商品库""",
  """连接 CRM""",
  """连接 CMS""",
  """连接数据库""",
]

ppt = [
  """封面：MCP""",
  """MCP 是什么""",
  """可连接系统示例""",
  """为什么它重要""",
  """安全边界""",
]

first_principles = [
  """先承认一个事实：模型参数里没有你公司的实时数据和系统权限。""",
  """连接层的存在，是为了让模型获得可控、可审计的外部能力。""",
  """MCP 的价值不是“多一个协议”，而是统一连接方式，降低系统集成复杂度。""",
]

atomic_truths = [
  """本质一：没有连接层，AI 很多时候只能停留在建议层。""",
  """本质二：连接真实系统的同时必须控制权限。""",
  """本质三：标准化连接方式比一次性对接更有长期价值。""",
]

method = [
  """先列业务里必须访问的系统。""",
  """按读取、写入、执行动作三类梳理权限。""",
  """对高风险动作加审批和日志。""",
]

anti_patterns = [
  """以为接入一个 API 就等于完成集成。""",
  """不给权限治理直接放 AI 执行动作。""",
  """每接一个系统都重复造一套连接逻辑。""",
]

[deep_note.meta]

[deep_note]
plain = """MCP 可以理解为 AI 连接外部工具和数据的标准接口层。没有连接，AI 很多时候只能给建议；有了连接，它才可能读取资料、调用工具、执行动作，但所有连接都必须带权限、日志和安全合规边界。"""
checks = [
  """这个流程需要连接哪些系统？""",
  """每个连接是读、写还是执行动作？""",
  """高风险动作有没有审批和日志？""",
]

[[deep_note.teacher_script]]
title = """先讲为什么要连接"""
body = """模型参数里没有你的实时库存、CRM 记录、CMS 草稿和内部数据库，所以需要可控连接。"""

[[deep_note.teacher_script]]
title = """MCP 统一连接方式"""
body = """与其每个系统都单独写一套对接，不如用统一协议描述工具、资源和调用方式。"""

[[deep_note.teacher_script]]
title = """连接不等于放权"""
body = """读取、写入、删除、发布是不同权限，高风险动作要审批和记录。"""

[deep_note.walkthrough]
scene = """让 AI 查询商品资料并生成上架草稿"""
steps = [
  """MCP 连接商品库，允许读取商品名称、规格、库存。""",
  """连接 CMS，只允许创建草稿，不允许直接发布。""",
  """AI 根据商品资料生成标题和详情页草稿。""",
  """运营人工核校后再发布。""",
]

[deep_note.worked_example]
