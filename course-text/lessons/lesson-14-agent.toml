# This file is editable lesson text. Run scripts/generate_lessons.py after changing it.
# Text fields use TOML triple-quoted strings so Chinese punctuation and line breaks are safe.

chapter_no = 4
no = 14
slug = """lesson-14-agent"""
title = """什么是 Agent"""
goal = """理解 Agent 为什么不是普通对话式交互模型，而是围绕目标、工具和反馈形成执行闭环的系统。"""
big_question = """为什么只靠对话式交互模型，很多任务会停留在建议层，而 Agent 能走向结果交付？"""
exercise = """选一个业务任务，画出对应 Agent 至少需要哪些工具和反馈环节。"""

keyPoints = [
  """Agent 至少包含目标、模型、工具、记忆、权限、反馈""",
  """Agent 能拆解任务、调用工具、观察结果并迭代修正""",
  """真正难的是治理而不是演示""",
]

structure = [
  """Agent 和对话式模型的区别""",
  """Agent 的六个部件""",
  """单 Agent 与多 Agent""",
  """治理与边界""",
]

cases = [
  """编程 Agent""",
  """客服 Agent""",
  """运营 Agent""",
]

ppt = [
  """封面：Agent""",
  """为什么需要 Agent""",
  """Agent 六部件""",
  """业务案例""",
  """治理边界""",
]

first_principles = [
  """先把“会回答”和“会执行”区分开。""",
  """Agent 的第一性价值在于把模型能力嵌进执行闭环。""",
  """所以 Agent 的关键不是模型看起来多聪明，而是它能否基于结果反馈修正动作。""",
]

atomic_truths = [
  """本质一：Agent 是系统，不是单一模型。""",
  """本质二：执行闭环比单次回答更重要。""",
  """本质三：Agent 越能动手，越需要治理。""",
]

method = [
  """设计 Agent 时先列六件事：目标、模型、工具、记忆、权限、反馈。""",
  """优先做能清楚验证的单 Agent 场景。""",
  """多 Agent 只有在分工明确时才有价值。""",
]

anti_patterns = [
  """把 Agent 当成会自动成功的黑盒。""",
  """先堆多个 Agent，再考虑协作逻辑。""",
  """只看执行能力，不看权限边界。""",
]

[deep_note.meta]

[deep_note]
plain = """Agent 不是更擅长对话的模型，而是围绕目标持续执行的系统。它需要目标、模型、工具、记忆、权限和反馈。越接近真实业务动作，越要限制权限、记录过程、设置人工确认。"""
checks = [
  """这个 Agent 的目标是否明确？""",
  """它能调用哪些工具，不能做哪些动作？""",
  """执行结果如何被观察和纠正？""",
]

[[deep_note.teacher_script]]
title = """先讲回答和执行的差别"""
body = """对话式模型主要给建议，Agent 会按目标拆步骤、调用工具、观察结果，再决定下一步。"""

[[deep_note.teacher_script]]
title = """Agent 是系统组合"""
body = """不要把 Agent 想成一个按钮，它至少包含模型、工具、记忆、权限和反馈机制。"""

[[deep_note.teacher_script]]
title = """治理比炫技重要"""
body = """能删文件、发消息、改订单的 Agent 必须有权限边界、日志、审批和可回滚方案。"""

[deep_note.walkthrough]
scene = """设计一个客服工单 Agent"""
steps = [
  """目标：把客户问题分流到正确处理队列。""",
  """工具：读取 FAQ、查询订单状态、创建工单。""",
  """权限：只能读订单状态，不能直接退款。""",
  """反馈：人工标记分流是否正确，用于改规则。""",
]

[deep_note.worked_example]
