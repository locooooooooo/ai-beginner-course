# This file is editable lesson text. Run scripts/generate_lessons.py after changing it.
# Text fields use TOML triple-quoted strings so Chinese punctuation and line breaks are safe.

chapter_no = 1
no = 3
slug = """lesson-03-model-families"""
title = """主流模型家族"""
goal = """理解不同模型家族的定位差异，避免把所有模型看成一回事。"""
big_question = """为什么同样是“AI 模型”，有人用来写代码，有人用来做图，有人用来做视频？"""
exercise = """从你的使用场景里选 3 个任务，分别判断更适合文本模型、图像模型还是视频模型。"""

keyPoints = [
  """OpenAI GPT 家族偏通用、多模态、工具调用""",
  """DeepSeek 偏推理、代码和中文使用场景""",
  """Claude 偏长文本理解与写作""",
  """Gemini 偏谷歌生态整合与多模态入口""",
  """图像模型可从 Midjourney、FLUX.1、DALL-E 3、Stable Diffusion 这类主流入口理解""",
  """视频模型可从 Kling、Runway Gen-3、Sora、Vidu 这类更容易检索和体验的入口理解""",
]

structure = [
  """为什么模型选型会影响结果""",
  """文本模型、图像模型、视频模型的区别""",
  """主流模型家族的定位""",
  """新手怎么选""",
]

cases = [
  """GPT 做通用问答、代码和 Agent 编排""",
  """DeepSeek 做推理问答和代码任务""",
  """Midjourney / FLUX.1 / DALL-E 3 / Stable Diffusion 做图像生成""",
  """Kling / Runway Gen-3 / Sora / Vidu 做视频生成""",
]

ppt = [
  """封面：主流模型家族""",
  """模型分类图""",
  """GPT 与 DeepSeek""",
  """Claude 与 Gemini""",
  """图像模型与视频模型""",
  """新手选型建议""",
]

first_principles = [
  """先按输入输出对象拆：文字、图片、视频、语音不是同一类问题。""",
  """再按业务目标拆：有的模型偏通用协同，有的偏推理代码，有的偏视觉生成。""",
  """最后按接入方式、成本与效果平衡来选，而不是按热度选。""",
]

atomic_truths = [
  """本质一：不同模型首先是处理对象不同。""",
  """本质二：模型强项来自训练目标和产品定位，不来自宣传词。""",
  """本质三：不存在“所有任务都最优”的模型，只有“当前任务更合适的模型”。""",
]

method = [
  """先判断任务是文本、图像、视频还是多模态混合任务。""",
  """再看任务最需要的是推理、创作、速度还是系统协同。""",
  """最后再比较是否能稳定接入你的工具链和业务流程。""",
]

anti_patterns = [
  """只按排行榜选模型。""",
  """把图像模型当成 LLM 使用。""",
  """不看任务类型，直接问“哪个模型最好”。""",
]

[deep_note.meta]
duration = """30-45 分钟"""
difficulty = """入门"""
tools = """可打开自己常用的 AI 产品列表；不要求注册新账号"""
privacy = """模型对比时只用公开任务，不上传未公开代码、合同、客户数据或私密图片。"""
last_reviewed = """2026-06-17"""
source_note = """模型家族和版本变化很快，本课只教选型思路；具体模型名称需要定期复核。"""

[deep_note]
plain = """不同模型不是简单谁更强，而是擅长处理的对象和任务不同。文本、图像、视频、代码、多模态各有强项，新手选模型时先看任务类型，再看效果、成本和接入方式。"""
checks = [
  """我能不能先说任务类型，而不是先说模型名称？""",
  """我能不能解释图像模型为什么不是传统 LLM？""",
  """我能不能为同一个项目拆出文本、图像、视频三个模型任务？""",
]

[[deep_note.teacher_script]]
title = """先按输入输出分类"""
body = """让学生先写输入是什么、想要输出什么。输入是文字、图片、表格、代码还是多种混合，输出是回答、图片、视频还是可执行文件。这样比问哪个模型最好更有用。"""

[[deep_note.teacher_script]]
title = """再按业务目标判断"""
body = """同样是文本模型，有的更适合通用协作，有的更适合代码，有的更适合长文档。选型不是追热点，而是看它是否稳定解决当前任务。"""

[[deep_note.teacher_script]]
title = """最后加入落地因素"""
body = """真实项目里还要看价格、速度、上下文长度、工具调用、隐私要求和团队能否接入。新手要尽早形成综合判断，而不是只看榜单。"""

[deep_note.walkthrough]
scene = """为一个活动做图文宣传"""
steps = [
  """写活动介绍：优先选择文本模型。""",
  """生成海报主视觉：选择图像模型。""",
  """做 10 秒短视频分镜：选择视频模型或文本模型先写脚本。""",
  """如果要自动发布：再考虑 Agent 和工具连接。""",
]

[deep_note.worked_example]
bad = """哪个模型最好？我想以后都用一个最强的。"""
good = """我先按任务选择模型：写活动文案用文本模型，生成海报用图像模型，做短视频分镜可以先用文本模型写脚本，再用视频模型生成镜头。选型要看输入输出、质量、速度、成本和能不能接入流程。"""
sample = """任务拆分：1. 活动说明和商品卖点整理：文本模型；2. 海报主视觉：图像模型；3. 15 秒短视频镜头：视频模型；4. 自动发布和检查：Agent 加工具连接。"""
review = [
  """是否先判断输入输出类型。""",
  """是否避免只按热度或排行榜选模型。""",
  """是否把同一项目拆成文本、图像、视频和流程任务。""",
]
