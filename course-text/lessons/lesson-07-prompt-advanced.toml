# This file is editable lesson text. Run scripts/generate_lessons.py after changing it.
# Text fields use TOML triple-quoted strings so Chinese punctuation and line breaks are safe.

chapter_no = 2
no = 7
slug = """lesson-07-prompt-advanced"""
title = """提示词进阶"""
goal = """掌握拆任务、分步骤和多轮追问，让复杂任务更稳。"""
big_question = """为什么复杂任务最容易失败？因为你把本该分阶段完成的工作一次性压给了模型。"""
exercise = """把“帮我做一个新品推广方案”拆成 4 个连续步骤，并写成多轮提示。"""

keyPoints = [
  """复杂任务不要一次性全塞给模型""",
  """可以先让模型澄清，再执行""",
  """分步骤输出通常更可靠""",
]

structure = [
  """为什么复杂任务容易失败""",
  """拆任务的方法""",
  """多轮追问技巧""",
  """先澄清再执行""",
]

cases = [
  """先列大纲再写文章""",
  """先确认平台规则再写文案""",
  """先检查缺失信息再输出结果""",
]

ppt = [
  """封面：提示词进阶""",
  """复杂任务拆解""",
  """多轮追问示例""",
  """澄清信息模板""",
  """练习页""",
]

first_principles = [
  """复杂任务失败的核心原因不是模型太差，而是问题没有被拆成稳定子问题。""",
  """先把任务拆到每一步都能验证，再决定交给模型怎么做。""",
  """让模型先澄清信息，本质上是在减少错误假设。""",
]

atomic_truths = [
  """本质一：可验证的子任务比一个笼统大任务更容易成功。""",
  """本质二：拆解顺序影响结果质量。""",
  """本质三：多轮交互的价值在于逐步降低不确定性。""",
]

method = [
  """先拆，再做；先问清，再执行。""",
  """给每一步设置中间产物，比如大纲、提纲、表格、草稿。""",
  """每一步只改一个关键变量，便于判断问题出在哪。""",
]

anti_patterns = [
  """把所有要求一口气砸给模型。""",
  """不给中间检查点。""",
  """模型一旦偏了，还沿着错误路径继续推进。""",
]

[deep_note.meta]
duration = """40-50 分钟"""
difficulty = """入门进阶"""
tools = """任意支持对话式交互的 AI 工具；最好使用同一对话连续多轮练习"""
privacy = """多轮练习中不要逐步补出敏感个人隐私、真实客户资料、内部预算或未公开投放策略。"""
last_reviewed = """2026-06-17"""
source_note = """本课强调任务拆解，不依赖特定模型。"""

[deep_note]
plain = """复杂任务要拆成多个可检查的小步骤。先澄清、再规划、再执行、再检查，比一次性让 AI 输出完整方案更稳定。"""
checks = [
  """我有没有把复杂任务拆成阶段？""",
  """每一阶段有没有可检查的中间产物？""",
  """我有没有先让 AI 澄清，而不是直接让它编完整方案？""",
]

[[deep_note.teacher_script]]
title = """先让学生看到一次性提问的风险"""
body = """复杂任务如果一次性问，模型会自动补很多假设。假设一错，后面整套方案都会偏。进阶提示词的核心是减少错误假设。"""

[[deep_note.teacher_script]]
title = """训练分阶段输出"""
body = """第一轮只让 AI 问缺失信息，第二轮让它给方案结构，第三轮写草稿，第四轮检查。每一步都有中间产物，错误更容易被发现。"""

[[deep_note.teacher_script]]
title = """把多轮变成流程"""
body = """进阶不是写更长的提示词，而是把任务流程化。能被重复执行的多轮链路，后面才可能沉淀成工作流或 Skill。"""

[deep_note.walkthrough]
scene = """做新品推广方案"""
steps = [
  """第 1 轮：请 AI 列出还缺哪些信息。""",
  """第 2 轮：补充预算、人群、渠道，让 AI 出方案大纲。""",
  """第 3 轮：选择一个方向，让 AI 写详细执行计划。""",
  """第 4 轮：让 AI 按目标、成本、风险、验收标准自检。""",
]

[deep_note.worked_example]
bad = """帮我做一个新品推广方案。"""
good = """我们分 4 轮完成新品推广方案。第 1 轮你只问我缺失信息；第 2 轮你根据我补充的信息输出方案大纲；第 3 轮你把我选中的方向写成执行计划；第 4 轮你按目标、预算、风险和验收标准做自检。现在先开始第 1 轮。"""
sample = """AI 第一轮应该反问：新品是什么、目标人群是谁、预算范围、推广渠道、上线时间、禁用表达、成功指标是什么，而不是直接生成完整方案。"""
review = [
  """是否先澄清缺失信息。""",
  """每一轮是否只完成一个阶段目标。""",
  """是否有最终自检和修正环节。""",
]
