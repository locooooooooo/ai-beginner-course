# This file is editable lesson text. Run scripts/generate_lessons.py after changing it.
# Text fields use TOML triple-quoted strings so Chinese punctuation and line breaks are safe.

chapter_no = 1
no = 2
slug = """lesson-02-llm-agent-mcp-skill"""
title = """大模型、LLM、Agent：先听懂分工"""
goal = """先用“语言处理核心、执行系统、连接接口、技能卡”这组比喻厘清 AI 系统分工，不急着啃协议细节。"""
big_question = """为什么很多人学了一堆名词，最后还是不会落地？因为没有厘清哪些概念负责语言处理、哪些负责执行、连接和复用。"""
visual_image = """../lesson-02-llm-agent-mcp-skill-visual.jpg"""
visual_image_alt = """LLM Agent 位于中间，左侧连接 MCP，右侧连接 Skill，展示理解执行、工具连接和方法复用之间的关系"""
visual_image_caption = """先把它当成四个工作角色：语言处理核心、执行系统、USB 接口和技能卡。MCP 与 Skill 的细节后面再展开。"""
visual_title = """一张图看懂：LLM、Agent、MCP、Skill 的通俗比喻"""
visual_warning = """本课不要求你掌握 MCP 协议细节，也不要求你会写 Skill。先把分工放进脑海：LLM 像语言处理核心，Agent 像执行系统，MCP 像 USB 接口，Skill 像工具箱。"""
exercise = """用“语言处理核心、执行系统、USB 接口、技能卡”这四个比喻解释 LLM、Agent、MCP、Skill，再举一个你能想到的真实业务例子。"""

keyPoints = [
  """大模型是 AI 中的一类基础模型""",
  """LLM 是擅长处理语言输入并生成语言输出的大模型""",
  """Agent 是会调工具、会执行流程的系统""",
  """MCP 本章只先理解成 AI 连接外部系统的数据管道""",
  """Skill 本章只先理解成可复用的专业技能卡或工具箱""",
]

structure = [
  """为什么要先分清这些词""",
  """从大到小的概念关系""",
  """每个概念的通俗比喻""",
  """MCP 和 Skill 为什么后面再细讲""",
]

cases = [
  """让 LLM 写文案草稿""",
  """让 Agent 按步骤发布文章""",
  """把商品库当作外接数据源""",
  """把电商 SOP 做成技能卡""",
]

ppt = [
  """封面：先听懂 AI 系统分工""",
  """一张通俗比喻图""",
  """LLM 为什么像语言处理核心""",
  """Agent 为什么像执行系统""",
  """MCP 为什么像 USB 接口""",
  """Skill 为什么像技能卡""",
  """不急着展开的内容""",
]

first_principles = [
  """先问每个概念解决的最小问题是什么，而不是先对照标准定义。""",
  """LLM 解决语言解析与生成问题，Agent 解决执行闭环问题，MCP 和 Skill 先用连接与复用两个比喻占位。""",
  """把四者放回同一条业务链路，先知道谁负责哪段，细节留到后续章节展开。""",
]

atomic_truths = [
  """本质一：名词存在的意义是分工，不是炫技。""",
  """本质二：业务落地通常会涉及语言处理、执行、连接、复用四件事，但新手要先抓主干。""",
  """本质三：单个模型能力不等于完整系统能力。""",
]

method = [
  """碰到新概念时，先问它更像语言处理核心、执行系统、接口还是技能卡。""",
  """做系统设计时，按四层分工思考，而不是一股脑扔给模型。""",
  """画系统图时，先把理解、执行、连接、复用拆开写清楚。""",
]

anti_patterns = [
  """把 Agent 和模型混成一个概念。""",
  """以为接了 MCP 就自动有业务流程。""",
  """把 Skill 当成高级提示词，而不是可复用资产。""",
]

[[visual_cards]]
label = """负责语言处理和生成"""
title = """LLM = 语言处理核心"""
body = """解析你的输入，再生成文字、代码、总结或方案。"""

[[visual_cards]]
label = """负责替你闭环"""
title = """Agent = 执行系统"""
body = """不只是回答一句话，而是按目标拆步骤、调用工具、检查结果。"""

[[visual_cards]]
label = """负责接外部系统"""
title = """MCP = USB 接口"""
body = """先把它理解成数据管道，让 AI 能读取文件、系统或工具。细节放到后面学。"""

[[visual_cards]]
label = """负责复用做法"""
title = """Skill = 技能卡"""
body = """把一类任务的步骤、模板、检查清单沉淀下来，下次不用从零开始。"""

[deep_note.meta]
duration = """30-40 分钟"""
difficulty = """零基础到入门"""
tools = """任意支持对话式交互的 AI 工具；建议准备一个自己熟悉的业务流程作例子"""
privacy = """流程示例可使用虚构商品、虚构客户和非机密数据；不要粘贴真实后台数据、内部接口或未公开业务规则。"""
last_reviewed = """2026-06-17"""
source_note = """本课讲概念分工，不绑定某个厂商实现。"""

[deep_note]
plain = """LLM 负责语言解析和生成，Agent 负责围绕目标执行，MCP 负责连接外部工具和数据，Skill 负责把可复用做法沉淀下来。四个词合在一起，才接近一个能落地的 AI 工作系统。"""
checks = [
  """我能不能用一句话说出 LLM 和 Agent 的区别？""",
  """我能不能指出一个流程里哪些地方需要 MCP？""",
  """我能不能把一个常做任务整理成 Skill 的雏形？""",
]

[[deep_note.teacher_script]]
title = """用分工而不是定义解释名词"""
body = """不要先让学生对照 LLM、Agent、MCP、Skill 的标准定义。先问：一个 AI 系统要真正辅助发布文章，需要解析内容、调用发布工具、读取素材库、复用发布 SOP。四个概念就是在这条链路里分工。"""

[[deep_note.teacher_script]]
title = """把四层画成一条业务链路"""
body = """LLM 生成草稿，Agent 决定下一步，MCP 连接 CMS 或商品库，Skill 固化标题检查、敏感词检查、发布前验收。学生看到链路后，名词就不再孤立。"""

[[deep_note.teacher_script]]
title = """强调缺一层会发生什么"""
body = """只有 LLM，容易停在建议；没有 MCP，碰不到真实系统；没有 Skill，下次同类任务又要从零解释；没有 Agent，就缺少连续执行和反馈。"""

[deep_note.walkthrough]
scene = """自动生成并发布一篇新品文章"""
steps = [
  """LLM：根据商品资料生成文章初稿。""",
  """Agent：按发布目标拆任务，决定先查资料、再写稿、再检查。""",
  """MCP：连接商品库、CMS、图片素材库。""",
  """Skill：复用团队的标题规则、品牌语气和发布验收清单。""",
]

[deep_note.worked_example]
bad = """LLM、Agent、MCP、Skill 都是 AI 工具，差不多都是让 AI 更强。"""
good = """LLM 负责语言解析和生成，Agent 负责围绕目标连续执行，MCP 负责连接外部系统和数据，Skill 负责把一类任务的做法沉淀成可复用资产。它们是同一条工作链路里的不同分工。"""
sample = """业务例子：自动发布新品文章。LLM 写初稿，Agent 拆解查资料、写稿、检查、发布草稿的步骤，MCP 连接商品库和 CMS，Skill 固化标题规则、品牌语气和发布前检查清单。"""
review = [
  """是否用分工解释名词，而不是堆定义。""",
  """是否能把四个概念放进同一个真实流程。""",
  """是否说明缺少某一层会出现什么问题。""",
]
