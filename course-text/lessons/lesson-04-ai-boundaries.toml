# This file is editable lesson text. Run scripts/generate_lessons.py after changing it.
# Text fields use TOML triple-quoted strings so Chinese punctuation and line breaks are safe.

chapter_no = 1
no = 4
slug = """lesson-04-ai-boundaries"""
title = """AI 能做什么不能做什么"""
goal = """建立边界意识，知道 AI 适合辅助哪些任务，也知道它可能在哪些环节出现事实性偏离。"""
big_question = """为什么 AI 输出看起来很自然，仍然可能一本正经地说错？"""
exercise = """列出你认为必须人工审批的 5 类 AI 输出，并说明原因。"""

keyPoints = [
  """AI 擅长总结、生成、归纳、辅助分析""",
  """AI 可能出现幻觉现象、上下文误配和事实性偏离""",
  """结果必须验证，尤其是高风险内容""",
]

structure = [
  """AI 的强项""",
  """AI 的常见失败方式""",
  """哪些任务必须人审""",
  """如何建立验证习惯""",
]

cases = [
  """写一篇文章很快但事实可能错""",
  """生成客服回复很自然但政策可能错""",
  """生成图片很快但价格和规格可能错""",
]

ppt = [
  """封面：AI 的边界""",
  """AI 擅长什么""",
  """AI 常见错误""",
  """高风险输出清单""",
  """验证方法""",
  """课堂讨论页""",
]

first_principles = [
  """先把“语言输出很自然”和“真的有事实依据”分开。""",
  """AI 的输出本质上是高概率生成，不天然等于事实正确。""",
  """高风险任务必须引入验证、审批和回滚，而不是只看语气是否自然。""",
]

atomic_truths = [
  """本质一：自然语言流畅性不等于真实性。""",
  """本质二：生成能力和责任能力不是同一回事。""",
  """本质三：越靠近金钱、健康、法律、售后，越要把验证前置。""",
]

method = [
  """先给输出分风险等级。""",
  """低风险结果先用，中风险结果抽检，高风险结果强制审批。""",
  """所有关键业务都要有验证路径。""",
]

anti_patterns = [
  """把自然流畅误判成可信。""",
  """让 AI 直接决定高风险动作。""",
  """遇到错误后只怪模型，不改流程。""",
]

[deep_note.meta]
duration = """30-40 分钟"""
difficulty = """入门"""
tools = """任意支持对话式交互的 AI 工具；建议准备一段低风险 AI 输出用于练习检查"""
privacy = """不要用真实医疗、法律、财务、客户投诉原文做公开练习；必须先进行数据脱敏，再使用虚拟数据或非机密数据演练。"""
last_reviewed = """2026-06-17"""
source_note = """边界课以通用风险治理为主，不替代法律、医疗、财务等专业意见。"""

[deep_note]
plain = """AI 能快速生成、总结和分析，但生成内容不天然等于事实，也不会替你承担责任。越靠近钱、法律、健康、售后、品牌承诺，越要把验证、审批和回滚放在前面。"""
checks = [
  """我能不能列出哪些 AI 输出必须人工审批？""",
  """我能不能说明自然流畅为什么不等于真实？""",
  """我能不能为一个高风险任务设计验证步骤？""",
]

[[deep_note.teacher_script]]
title = """先拆开流畅和正确"""
body = """AI 的输出常常很顺，但顺不代表真。课堂上可以给一段看起来很像官方说明的文字，让学生找事实来源，训练他们不要被语气骗过。"""

[[deep_note.teacher_script]]
title = """用风险等级决定能不能直接用"""
body = """低风险内容可以快速试用，中风险内容要抽检，高风险内容必须人工审批。边界意识不是不用 AI，而是知道哪些地方不能让它直接决定。"""

[[deep_note.teacher_script]]
title = """把错误看成流程问题"""
body = """AI 输出出现事实性偏离后，不要只归因于模型。要追问是输入缺上下文、知识库没接、核校标准没写，还是人工核校流程缺失。"""

[deep_note.walkthrough]
scene = """让 AI 生成售后政策回复"""
steps = [
  """先判断风险：这涉及客户权益和品牌承诺，不能直接发。""",
  """提供依据：给 AI 官方售后政策和适用范围。""",
  """输出草稿：要求标出引用条款。""",
  """人工核校：客服负责人确认后再使用。""",
]

[deep_note.worked_example]
bad = """AI 写的售后回复很专业，可以直接发给客户。"""
good = """售后回复涉及客户权益，不能直接发。我要先提供官方售后政策，让 AI 只生成草稿，并要求标出依据条款、适用条件和不确定项。最后由客服负责人核校后再发送。"""
sample = """风险核校结果：文案里如果出现“必定赔偿”“任何情况都可退”这类政策外承诺，必须删除；如果政策文档没有覆盖该情况，要转人工确认，不能让模型补写规则。"""
review = [
  """是否区分流畅表达和真实依据。""",
  """是否给高风险任务设置人工审批。""",
  """是否能写出验证来源、适用范围和转人工条件。""",
]
